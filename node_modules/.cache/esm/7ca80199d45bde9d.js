let nodemailer,SMTP_MAIL,SMTP_PASSWORD;_d45‍.x([["default",()=>_d45‍.o]]);_d45‍.w("nodemailer",[["default",["nodemailer"],function(v){nodemailer=v}]]);_d45‍.w("../config",[["SMTP_MAIL",["SMTP_MAIL"],function(v){SMTP_MAIL=v}],["SMTP_PASSWORD",["SMTP_PASSWORD"],function(v){SMTP_PASSWORD=v}]]);


const sendmail = async (email,mailsubject,content)=>{
    try {
        nodemailer.createTransport({
            host:"smtp.gmail.com",
            port:587,
            secure:true,
            requireTLS:false,
            auth:{
                user:SMTP_MAIL,
                pass:SMTP_PASSWORD
            }
        });

        const mailOptions = {
            from:SMTP_MAIL,
            to:email,
            subject:mailsubject,
            html:content
        }

        transport.sendmail(mailsubject,function(error,info){
            if(error){
                _d45‍.g.console.log(error);
            }else{
                _d45‍.g.console.log('mail sent successfully- ',info.response);
            }
        })

    } catch (error) {
        _d45‍.g.console.log(error.massage);
        
    }
}

_d45‍.d(sendmail);
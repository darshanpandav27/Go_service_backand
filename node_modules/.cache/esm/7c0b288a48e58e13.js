let CustomErrorHandler,JwtService;_48e‍.x([["default",()=>_48e‍.o]]);_48e‍.w("../services/CustomErrorHandler.js",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_48e‍.w("../services/JwtService.js",[["default",["JwtService"],function(v){JwtService=v}]]);


const auth = async (req, res, next) => {
    let authHeader = req.headers.authorization;
    if (!authHeader) {
        return next(CustomErrorHandler.unAuthorized());
    }

    const token = authHeader.split(' ')[1];

    try {
        const { _id, role } = await JwtService.verify(token);
        const user = {
            _id,
            role
        }
        req.user = user;
        next();

    } catch(err) {
        return next(CustomErrorHandler.unAuthorized());
    }

}

_48e‍.d(auth);
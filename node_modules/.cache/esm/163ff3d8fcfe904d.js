let User,multer,CustomErrorHandler,path,fs,profileSchema,AdminprofileSchema;_fcf‍.x([["default",()=>_fcf‍.o]]);_fcf‍.w("../models/index.js",[["User",["User"],function(v){User=v}]]);_fcf‍.w("multer",[["default",["multer"],function(v){multer=v}]]);_fcf‍.w("../services/CustomErrorHandler.js",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_fcf‍.w("path",[["default",["path"],function(v){path=v}]]);_fcf‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_fcf‍.w("../validators/profileValidator.js",[["default",["profileSchema"],function(v){profileSchema=v}]]);_fcf‍.w("../validators/adminValidator.js",[["default",["AdminprofileSchema"],function(v){AdminprofileSchema=v}]]);







const storage = multer.diskStorage({
    destination: (req, file, cb) => cb(null, 'uploads'),
    filename: (req, file, cb) => {
        const uniqueName = `${Date.now()}-${Math.round(
            Math.random() * 1e9
        )}${path.extname(file.originalname)}`;
        // 3746674586-836534453.png
        cb(null, uniqueName);
    },
});

const handleMultipartData = multer({
    storage,
    limits: { fileSize: 1000000 * 5 },
}).single('image'); // 5mb



const profileController = {
    async profile(req, res, next) {
        handleMultipartData(req, res, async (err) => {
            if (err) {
                return res.status(500).json({
                    status: false,
                    massage: 'Internal server error'
                });
            }
            const filePath = req.file.path;
            // validation

            const { error } = profileSchema.validate(req.body);

             if(error){
                fs.unlink(`${appRoot}/${filePath}`,(err)=>{
                    // console.log(err.message);
                        return res.status(500).json({
                            status: false,
                            massage: 'Internal server error'
                        });
                });
                return res.status(500).json({
                    status: false,
                    massage: error
                });
             }


            const { name, email, address, Gender,phone_number} = req.body;

            let document;
            try {
                document = await User.findOneAndUpdate(
                    {_id: req.params.id},
                    {name,
                    email,
                    phone_number,
                    address,
                    Gender,
                    image: filePath},
                    {new : true}
                );
                _fcf‍.g.console.log(document.image);
                if (!document) {
                    return res.status(400).json({
                        status: false,
                        massage:"404 Not Found"
                    });
                }
            } catch (err) {
                return res.status(500).json({
                    status: false,
                    massage: err
                });
            }
            _fcf‍.g.console.log(document);
            res.status(200).json({
                status: true,
                data:document,
                massage:"successfully created..."
            });
        });
    },
    async Adminprofile(req, res, next) {
        handleMultipartData(req, res, async (err) => {
            if (err) {
                return res.status(500).json({
                    status: false,
                    massage: 'Internal server error'
                });
            }
            const filePath = req.file.path;
            // validation

            const { error } = AdminprofileSchema.validate(req.body);

             if(error){
                fs.unlink(`${appRoot}/${filePath}`,(err)=>{
                    // console.log(err.message);
                        return res.status(500).json({
                            status: false,
                            massage: 'Internal server error'
                        });
                });
                return res.status(500).json({
                    status: false,
                    massage: error
                });
             }


            const { name, email, address,phone_number,Lat,Log,Compny_name} = req.body;

            let document;
            try {
                document = await User.findOneAndUpdate(
                    {_id: req.params.id},
                    {name,
                    phone_number,
                    address,
                    Lat,
                    Log,
                    Compny_name,
                    image: filePath},
                    {new : true}
                );
                _fcf‍.g.console.log(document.image);
                if (!document) {
                    return res.status(400).json({
                        status: false,
                        massage:"404 Not Found"
                    });
                }
            } catch (err) {
                return res.status(500).json({
                    status: false,
                    massage: err
                });
            }
            _fcf‍.g.console.log(document);
            res.status(200).json({
                status: true,
                data:document,
                massage:"successfully created..."
            });
        });
    },
    async update(req, res, next) {
        handleMultipartData(req, res, async (err) => {
            if (err) {
                return res.status(500).json({
                    status: false,
                    massage: 'Internal server error'
                });
            }
            let filePath;
            if (req.file) {
                filePath = req.file.path;
            }
            // validation

            const { error } = profileSchema.validate(req.body);

             if(error){
                fs.unlink(`${appRoot}/${filePath}`,(err)=>{
                    // console.log(err.message);
                         return res.status(500).json({
                            status: false,
                            massage: 'Internal server error'
                        });
                });
                return res.status(500).json({
                    status: false,
                    massage: error
                });
             }


            const { name, email, address, Gender,phone_number} = req.body;

            let document;
            try {
                document = await User.findOneAndUpdate(
                    {_id: req.params.id},
                    {name,
                    email,
                    phone_number,
                    address,
                    Gender,
                    ...(req.file && { image: filePath })},
                    { new : true}
                ).select('-password -updatedAt -__v');
                if (!document) {
                    return next(CustomErrorHandler.notFound());
                }
            } catch (err) {
                return next(err).json({
                    status: false,
                    massage:err
                });
            }
            res.status(200).json({
                status: true,
                data:document,
                massage:"Update successfully"
            });
        });
    },
    async Adminprofileupdate(req, res, next) {
        handleMultipartData(req, res, async (err) => {
            if (err) {
                return res.status(500).json({
                    status: false,
                    massage: 'Internal server error'
                });
            }
            const filePath = req.file.path;
            // validation

            const { error } = AdminprofileSchema.validate(req.body);

             if(error){
                fs.unlink(`${appRoot}/${filePath}`,(err)=>{
                    // console.log(err.message);
                        return res.status(500).json({
                            status: false,
                            massage: 'Internal server error'
                        });
                });
                return res.status(500).json({
                    status: false,
                    massage: error
                });
             }


            const { name, email, address,phone_number,Compny_name} = req.body;

            let document;
            try {
                document = await User.findOneAndUpdate(
                    {_id: req.params.id},
                    {name,
                    phone_number,
                    address,
                    Compny_name,
                    image: filePath},
                    {new : true}
                );
                _fcf‍.g.console.log(document.image);
                if (!document) {
                    return res.status(400).json({
                        status: false,
                        massage:"404 Not Found"
                    });
                }
            } catch (err) {
                return res.status(500).json({
                    status: false,
                    massage: err
                });
            }
            _fcf‍.g.console.log(document);
            res.status(200).json({
                status: true,
                data:document,
                massage:"successfully created..."
            });
        });
    },
    async fulldata(req, res, next) {
        try {
            const user = await User.findById({ _id: req.user._id }).populate("sevice_id").select('-password -updatedAt -__v');
            if (!user) {
                return res.status(400).json({
                    status: false,
                    massage:"404 Not Found"
                });
            }
            res.status(200).json({
                status:true,
               data: user,
               massage:"successfully"
            })
        } catch(err) {
           return next(err);
        }
    },
}

_fcf‍.d(profileController);
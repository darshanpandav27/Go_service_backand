let Joi,User,bcrypt,JwtService,CustomErrorHandler,randomstring,sendmail;_9f6‍.x([["default",()=>_9f6‍.o]]);_9f6‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_9f6‍.w("../../models",[["User",["User"],function(v){User=v}]]);_9f6‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_9f6‍.w("../../services/JwtService",[["default",["JwtService"],function(v){JwtService=v}]]);_9f6‍.w("../../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_9f6‍.w("randomstring",[["default",["randomstring"],function(v){randomstring=v}]]);_9f6‍.w("../../services/Otpsending",[["default",["sendmail"],function(v){sendmail=v}]]);







const registerController = {
    async register(req, res, next) {

        const registerSchema = Joi.object({
            name: Joi.string().min(3).max(30).required(),
            email: Joi.string().email().required(),
            role: Joi.string(),
            phone_number: Joi.string().min(10).required(),
            password: Joi.string().pattern(new RegExp('^[a-zA-Z0-9]{3,30}$')).required(),
        });

        _9f6‍.g.console.log(req.body);

        const { error } = registerSchema.validate(req.body);
        
        if (error) {
            return res.status(500).json({
                status: false,
                massage: error
            })
        }

        try {
            const exist = await User.exists({ email: req.body.email });
            if (exist) {
                return res.status(409).json({
                    status: false,
                    massage:'This email is already taken.'
                })
            }
        } catch(err) {
            return res.status(500).json({
                status: false,
                massage: err
            })
        }

        try {
            const exist = await User.exists({ phone_number: req.body.phone_number });
            if (exist) {
                return res.status(409).json({
                    status: false,
                    massage:'This Phone Number is already taken.'
                })
            }
        } catch(err) {
            return res.status(500).json({
                status: false,
                massage: err
            })
        }

        const { name, email, phone_number ,password,role } = req.body;

    // Hash password
    const hashedPassword = await bcrypt.hash(password, 10);

    // prepare the model

    const user = new User({
        name,
        email,
        role,
        phone_number,
        password: hashedPassword
    });

    let access_token;

    try {
        // let mailsubject = "Mail Verification";
        // const randomsTring = randomstring.generate({
        // length: 6,
        // charset: ['numeric']
        // });
        // console.log(randomsTring);
        // let content = ' Hello, '+name+',\n \n'+randomsTring+'';
        // sendmail(req.body.email,mailsubject,content);
        const result = await user.save();
        _9f6‍.g.console.log(result);
        // Token
        access_token = JwtService.sign({ _id: result._id, role: result.role });
        // database whitelist
    } catch(err) {
        return res.status(500).json({
            status: false,
            massage: err
        })
    }
    
    res.status(200).json({ status:true,user,access_token,massage:"login successful... "});

    },
    async otp(req,res,next){
        
        const otpSchema = Joi.object({
            name: Joi.string().min(3).max(30).required(),
            email: Joi.string().email().required()
        });

        _9f6‍.g.console.log(req.body);

        const { error } = otpSchema.validate(req.body);

        if (error) {
            return res.status(500).json({
                status: false,
                massage: error
            })
        }

        try {
            const user = await User.findOne({ email: req.body.email });
            if (!user) {
                return res.status(409).json({
                    status: false,
                    massage:'Email is wrong !'
                })
            }
        } catch(err) {
            return res.status(500).json({
                status: false,
                massage: err
            })
        }

        try {
            let mailsubject = "Mail Verification";
            const otp = randomstring.generate({
            length: 6,
            charset: ['numeric']
            });
            _9f6‍.g.console.log(otp);
            let content = ' Hello, '+req.body.name+',\n \n'+otp+'';
            sendmail(req.body.email,mailsubject,content);
            const user = await User.findOneAndUpdate({ _id: req.user._id }, {otp}).select('-password -updatedAt -__v');
            if (!user) {
                returnres.status(400).json({
                    status: false,
                    massage:"404 Not Found"
                });
            }
            const result = await user.save();
            _9f6‍.g.console.log(result);
            res.status(200).json({ status:true,otp,massage:"Otp Sending...."});
            // Token
            // database whitelist
        } catch(err) {
            return res.status(500).json({
                status: false,
                massage: err
            })
        }
        
        
    }
}


_9f6‍.d(registerController);1
let User,multer,CustomErrorHandler,path,fs,profileSchema,appRoot;_cd3‍.x([["default",()=>_cd3‍.o]]);_cd3‍.w("../models",[["User",["User"],function(v){User=v}]]);_cd3‍.w("multer",[["default",["multer"],function(v){multer=v}]]);_cd3‍.w("../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_cd3‍.w("path",[["default",["path"],function(v){path=v}]]);_cd3‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_cd3‍.w("../validators/profileValidator",[["default",["profileSchema"],function(v){profileSchema=v}]]);_cd3‍.w("../config",[["appRoot",["appRoot"],function(v){appRoot=v}]]);







const storage = multer.diskStorage({
    destination: (req, file, cb) => cb(null, 'uploads'),
    filename: (req, file, cb) => {
        const uniqueName = `${Date.now()}-${Math.round(
            Math.random() * 1e9
        )}${path.extname(file.originalname)}`;
        // 3746674586-836534453.png
        cb(null, uniqueName);
    },
});

const handleMultipartData = multer({
    storage,
    limits: { fileSize: 1000000 * 5 },
}).single('image'); // 5mb


const profileController = {
    async profile(req, res, next) {
        handleMultipartData(req, res, async (err) => {
            if (err) {
                return res.status(500).json({
                    status: false,
                    massage: 'Internal server error'
                });
            }
            const filePath = req.file.path;
            // validation

            const { error } = profileSchema.validate(req.body);

             if(error){
                fs.unlink(`${appRoot}/${filePath}`,(err)=>{
                    // console.log(err.message);
                        return res.status(500).json({
                            status: false,
                            massage: 'Internal server error'
                        });
                });
                return res.status(500).json({
                    status: false,
                    massage: error
                });
             }


            const { name, email, address, Gender,phone_number} = req.body;

            let document;
            try {
                document = await User.findOneAndUpdate(
                    {_id: req.params.id},
                    {name,
                    email,
                    phone_number,
                    address,
                    Gender,
                    image: filePath},
                    {new : true}
                );
                if (!document) {
                    return res.status(400).json({
                        status: false,
                        massage:"404 Not Found"
                    });
                }
            } catch (err) {
                return res.status(500).json({
                    status: false,
                    massage: err
                });
            }
            _cd3‍.g.console.log(document);
            res.status(200).json({
                status: true,
                data:document,
                massage:"successfully created..."
            });
        });
    },
    update(req, res, next) {
        handleMultipartData(req, res, async (err) => {
            if (err) {
                return res.status(500).json({
                    status: false,
                    massage: 'Internal server error'
                });
            }
            let filePath;
            if (req.file) {
                filePath = req.file.path;
            }
            // validation

            const { error } = profileSchema.validate(req.body);

             if(error){
                fs.unlink(`${appRoot}/${filePath}`,(err)=>{
                    // console.log(err.message);
                         return res.status(500).json({
                            status: false,
                            massage: 'Internal server error'
                        });
                });
                return res.status(500).json({
                    status: false,
                    massage: error
                });
             }


            const { name, email, address, Gender,phone_number} = req.body;

            let document;
            try {
                document = await User.findOneAndUpdate(
                    {_id: req.params.id},
                    {name,
                    email,
                    phone_number,
                    address,
                    Gender,
                    ...(req.file && { image: filePath })},
                    { new : true}
                ).select('-password -updatedAt -__v');
                if (!document) {
                    return next(CustomErrorHandler.notFound());
                }
            } catch (err) {
                return next(err).json({
                    status: false,
                    massage:err
                });
            }
            res.status(200).json({
                status: true,
                data:document,
                massage:"Update successfully"
            });
        });
    }
}

_cd3‍.d(profileController);
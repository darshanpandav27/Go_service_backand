let nodemailer,SMTP_MAIL,SMTP_PASSWORD;_afe‍.x([["default",()=>_afe‍.o]]);_afe‍.w("nodemailer",[["default",["nodemailer"],function(v){nodemailer=v}]]);_afe‍.w("../config",[["SMTP_MAIL",["SMTP_MAIL"],function(v){SMTP_MAIL=v}],["SMTP_PASSWORD",["SMTP_PASSWORD"],function(v){SMTP_PASSWORD=v}]]);


const sendmail = async (email,mailsubject,content)=>{
    try {
        var transporter = nodemailer.createTransport({
            service: 'gmail',
            auth: {
              user: SMTP_MAIL,
              pass: SMTP_PASSWORD
            }
          });
          
          const mailOptions = {
              from:SMTP_MAIL,
              to:email,
              subject: mailsubject,
                text: content
            } 
          
          transporter.sendMail(mailOptions, function(error, info){
            if (error) {
              _afe‍.g.console.log(error);
            } else {
              _afe‍.g.console.log('Email sent: ' + info.response);
            }
          });

    } catch (error) {
        _afe‍.g.console.log(error.massage);
        
    }
}

_afe‍.d(sendmail);
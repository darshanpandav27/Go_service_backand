let JWT_SECRET,jwt;_db7‍.x([["default",()=>_db7‍.o]]);_db7‍.w("../config/index.js",[["JWT_SECRET",["JWT_SECRET"],function(v){JWT_SECRET=v}]]);_db7‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);


class JwtService {
    static sign(payload, expiry = '5y', secret = JWT_SECRET) {
        return jwt.sign(payload, secret, { expiresIn: expiry });
    }

    static verify(token, secret = JWT_SECRET) {
        return jwt.verify(token, secret);
    }
}

_db7‍.d(JwtService);